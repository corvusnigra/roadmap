---
schemaVersion: 1
slug: java-jdk-jre-jvm
title: "JDK, JRE, JVM — три уровня платформы"
summary: "Что я скачиваю, что я запускаю, что исполняет мой код?"
status: published
estimatedMinutes: 20
prerequisites: []
learningOutcomes:
  - "Отличаешь JDK/JRE/JVM в одном предложении"
  - "Знаешь, что такое bytecode и где JIT встраивается"
  - "Можешь объяснить разницу OpenJDK vs Oracle JDK"
  - "Выполнять регулярно: Скомпилировать Hello.java через javac, запустить через java"
  - "Выполнять регулярно: Снять thread dump через jstack у живого процесса"
practice:
  - kind: mcq
    prompt: "По какому из признаков честно судить, что дисциплина «JDK, JRE, JVM — три уровня платформы» начала работать?"
    options:
      - "Отличаешь JDK/JRE/JVM в одном предложении"
      - "«JVM = Java» — путать платформу с языком"
      - "«Java интерпретируется» — забывать про JIT"
      - "Думать, что JDK 17 заменяет JDK 11 (LTS живут параллельно)"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение, которое появляется регулярно. «Отличаешь JDK/JRE/JVM в одном предложении» — именно такой признак."
flashcards:
  - front: "Что стоит за различением «JVM (спецификация + рантайм) ↔ JRE (рантайм + std-lib) ↔ JDK (JRE + tooling)»?"
    back: "JVM (спецификация + рантайм) ↔ JRE (рантайм + std-lib) ↔ JDK (JRE + tooling). JVM — спецификация виртуальной машины (загрузчик классов + интерпретатор bytecode + JIT + GC + memory model)"
  - front: "Что стоит за различением «javac → bytecode (.class) → JVM интерпретирует и JIT-компилирует»?"
    back: "javac → bytecode (.class) → JVM интерпретирует и JIT-компилирует. JRE — JVM + стандартная библиотека классов (java.lang, java.util, java.nio, ...) — минимум для запуска уже скомпилированной программы"
  - front: "Что стоит за различением «LTS (Java 8, 11, 17, 21) ↔ non-LTS (9, 10, 12...)»?"
    back: "LTS (Java 8, 11, 17, 21) ↔ non-LTS (9, 10, 12...). JDK — JRE + инструменты разработки (javac, javadoc, jdb, jconsole, jmap, jstack)"
  - front: "Что стоит за различением «Oracle JDK ↔ OpenJDK (один кодовый source, разная сборка)»?"
    back: "Oracle JDK ↔ OpenJDK (один кодовый source, разная сборка). Начиная с Java 11 отдельного JRE Oracle не поставляет — есть только JDK; JRE собирают сами через jlink"
  - front: "По какому маркеру понять, что освоено: «Отличаешь JDK/JRE/JVM в одном предложении»?"
    back: "Отличаешь JDK/JRE/JVM в одном предложении"
  - front: "По какому маркеру понять, что освоено: «Знаешь, что такое bytecode и где JIT встраивается»?"
    back: "Знаешь, что такое bytecode и где JIT встраивается"
masteryQuiz:
  - kind: mcq
    prompt: "Какой вопрос ставит дисциплина «JDK, JRE, JVM — три уровня платформы» в центр?"
    options:
      - "Что я скачиваю, что я запускаю, что исполняет мой код?"
      - "«JVM = Java» — путать платформу с языком"
      - "«Java интерпретируется» — забывать про JIT"
      - "Думать, что JDK 17 заменяет JDK 11 (LTS живут параллельно)"
    answerIndex: 0
    explanation: "Дисциплина задаёт фокусирующий вопрос — он определяет, что считать работой, а что — её имитацией. Здесь это: «Что я скачиваю, что я запускаю, что исполняет мой код?»"
  - kind: mcq
    prompt: "Что из перечисленного — антипаттерн дисциплины «JDK, JRE, JVM — три уровня платформы»?"
    options:
      - "«JVM = Java» — путать платформу с языком"
      - "Скомпилировать Hello.java через javac, запустить через java"
      - "Отличаешь JDK/JRE/JVM в одном предложении"
      - "Знаешь, что такое bytecode и где JIT встраивается"
    answerIndex: 0
    explanation: "Антипаттерн — типовой способ имитировать дисциплину, не осваивая её. В материалах он отмечен явно: ««JVM = Java» — путать платформу с языком»"
  - kind: mcq
    prompt: "Какое из утверждений — ключевое различение в «JDK, JRE, JVM — три уровня платформы»?"
    options:
      - "JVM (спецификация + рантайм) ↔ JRE (рантайм + std-lib) ↔ JDK (JRE + tooling)"
      - "«JVM = Java» — путать платформу с языком"
      - "«Java интерпретируется» — забывать про JIT"
      - "Думать, что JDK 17 заменяет JDK 11 (LTS живут параллельно)"
    answerIndex: 0
    explanation: "Различение «JVM (спецификация + рантайм) ↔ JRE (рантайм + std-lib) ↔ JDK (JRE + tooling)» лежит в основе дисциплины и определяет, как видеть её объекты."
  - kind: mcq
    prompt: "Какое действие в основе регулярной практики «JDK, JRE, JVM — три уровня платформы»?"
    options:
      - "Скомпилировать Hello.java через javac, запустить через java"
      - "«JVM = Java» — путать платформу с языком"
      - "«Java интерпретируется» — забывать про JIT"
      - "Думать, что JDK 17 заменяет JDK 11 (LTS живут параллельно)"
    answerIndex: 0
    explanation: "Регулярная практика — то, что превращает знание в навык. Здесь это: «Скомпилировать Hello.java через javac, запустить через java»"
  - kind: mcq
    prompt: "По какому маркеру судить, что «JDK, JRE, JVM — три уровня платформы» работает?"
    options:
      - "Отличаешь JDK/JRE/JVM в одном предложении"
      - "«JVM = Java» — путать платформу с языком"
      - "«Java интерпретируется» — забывать про JIT"
      - "Думать, что JDK 17 заменяет JDK 11 (LTS живут параллельно)"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение: «Отличаешь JDK/JRE/JVM в одном предложении». Не ощущение, а действие."
  - kind: mcq
    prompt: "Какой инструмент использует дисциплина «JDK, JRE, JVM — три уровня платформы» как опору?"
    options:
      - "javac, java, jar, jlink, jdeps"
      - "«JVM = Java» — путать платформу с языком"
      - "«Java интерпретируется» — забывать про JIT"
      - "Думать, что JDK 17 заменяет JDK 11 (LTS живут параллельно)"
    answerIndex: 0
    explanation: "Шаблон или инструмент удерживает дисциплину между сессиями. Здесь это: «javac, java, jar, jlink, jdeps»"
---

## Зачем

JVM — спецификация виртуальной машины (загрузчик классов + интерпретатор bytecode + JIT + GC + memory model). JRE — JVM + стандартная библиотека классов (java.lang, java.util, java.nio, ...) — минимум для запуска уже скомпилированной программы. JDK — JRE + инструменты разработки (javac, javadoc, jdb, jconsole, jmap, jstack). Начиная с Java 11 отдельного JRE Oracle не поставляет — есть только JDK; JRE собирают сами через jlink. Дистрибутивы (OpenJDK / Eclipse Temurin / Amazon Corretto / Azul Zulu) различаются вендором сборки, поддержкой LTS, и набором GC.

## Ключевые различения

- JVM (спецификация + рантайм) ↔ JRE (рантайм + std-lib) ↔ JDK (JRE + tooling)
- javac → bytecode (.class) → JVM интерпретирует и JIT-компилирует
- LTS (Java 8, 11, 17, 21) ↔ non-LTS (9, 10, 12...)
- Oracle JDK ↔ OpenJDK (один кодовый source, разная сборка)

## Технология

- javac, java, jar, jlink, jdeps
- jcmd, jstack, jmap, jstat, jconsole, jconsole
- SDKMAN! для управления версиями

## Практика (упражнения)

- Скомпилировать Hello.java через javac, запустить через java
- Снять thread dump через jstack у живого процесса
- Через jlink собрать минимальный JRE под своё приложение

## Маркеры освоения

- Отличаешь JDK/JRE/JVM в одном предложении
- Знаешь, что такое bytecode и где JIT встраивается
- Можешь объяснить разницу OpenJDK vs Oracle JDK

## Антипаттерны

- «JVM = Java» — путать платформу с языком
- «Java интерпретируется» — забывать про JIT
- Думать, что JDK 17 заменяет JDK 11 (LTS живут параллельно)

## Где в стеке

**Уровень 0: Java Core.** Фундамент языка. Без него все следующие уровни рассыпаются: equals/hashCode не работает в HashMap, Stream API ломается на peek без терминалки, String Pool путается с new String.
