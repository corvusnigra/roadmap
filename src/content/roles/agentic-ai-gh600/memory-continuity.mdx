---
schemaVersion: 1
slug: memory-continuity
title: "Непрерывность памяти и состояния"
summary: "Как обеспечить общий state между инструментами и средами без устаревания и конфликтов?"
status: published
estimatedMinutes: 25
prerequisites: []
learningOutcomes:
  - "Знаешь, где живёт каждый kind state твоих агентов"
  - "Конфликты состояния ловятся до prod, не в prod"
  - "Выполнять регулярно: Любое cross-environment действие — с явным state handle (commit / artifact id)"
  - "Выполнять регулярно: Один файл = один source-of-truth для конфига"
  - "Выполнять регулярно: Регулярная sweep на «брошенный» state (старые branches, кэши)"
practice:
  - kind: mcq
    prompt: "По какому из признаков честно судить, что дисциплина «Непрерывность памяти и состояния» начала работать?"
    options:
      - "Знаешь, где живёт каждый kind state твоих агентов"
      - "Несколько источников правды для одного факта"
      - "Cache без явной инвалидации"
      - "State, мигрирующий между средами «как-нибудь»"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение, которое появляется регулярно. «Знаешь, где живёт каждый kind state твоих агентов» — именно такой признак."
flashcards:
  - front: "Что стоит за различением «Shared state (один источник правды) ↔ replicated (копии в разных местах)»?"
    back: "Shared state (один источник правды) ↔ replicated (копии в разных местах). Когда задача проходит через несколько tools и сред (локальный editor → CI → production), state должен быть source-of-truth и явно передаваться, а не «всплывать» в каждом контексте"
  - front: "Что стоит за различением «Конфликт (два источника не согласны) ↔ устаревание (один отстал)»?"
    back: "Конфликт (два источника не согласны) ↔ устаревание (один отстал). Конфликтующий контекст = два tool'а имеют разные snapshots"
  - front: "Что стоит за различением «Версионированный state (commit hash) ↔ free-form»?"
    back: "Версионированный state (commit hash) ↔ free-form. Устаревший контекст = state из прошлой задачи влияет на текущую."
  - front: "Что стоит за различением «Read-through cache ↔ stale cache»?"
    back: "Read-through cache ↔ stale cache. Когда задача проходит через несколько tools и сред (локальный editor → CI → production), state должен быть source-of-truth и явно передаваться, а не «всплывать» в каждом контексте. Конфликтующий контекст = два tool'а имеют разные snapshots. Устаревший контекст = state из прошлой задачи влияет на текущую."
  - front: "По какому маркеру понять, что освоено: «Знаешь, где живёт каждый kind state твоих агентов»?"
    back: "Знаешь, где живёт каждый kind state твоих агентов"
  - front: "По какому маркеру понять, что освоено: «Конфликты состояния ловятся до prod, не в prod»?"
    back: "Конфликты состояния ловятся до prod, не в prod"
masteryQuiz:
  - kind: mcq
    prompt: "Какой вопрос ставит дисциплина «Непрерывность памяти и состояния» в центр?"
    options:
      - "Как обеспечить общий state между инструментами и средами без устаревания и конфликтов?"
      - "Несколько источников правды для одного факта"
      - "Cache без явной инвалидации"
      - "State, мигрирующий между средами «как-нибудь»"
    answerIndex: 0
    explanation: "Дисциплина задаёт фокусирующий вопрос — он определяет, что считать работой, а что — её имитацией. Здесь это: «Как обеспечить общий state между инструментами и средами без устаревания и конфликтов?»"
  - kind: mcq
    prompt: "Что из перечисленного — антипаттерн дисциплины «Непрерывность памяти и состояния»?"
    options:
      - "Несколько источников правды для одного факта"
      - "Любое cross-environment действие — с явным state handle (commit / artifact id)"
      - "Знаешь, где живёт каждый kind state твоих агентов"
      - "Конфликты состояния ловятся до prod, не в prod"
    answerIndex: 0
    explanation: "Антипаттерн — типовой способ имитировать дисциплину, не осваивая её. В материалах он отмечен явно: «Несколько источников правды для одного факта»"
  - kind: mcq
    prompt: "Какое из утверждений — ключевое различение в «Непрерывность памяти и состояния»?"
    options:
      - "Shared state (один источник правды) ↔ replicated (копии в разных местах)"
      - "Несколько источников правды для одного факта"
      - "Cache без явной инвалидации"
      - "State, мигрирующий между средами «как-нибудь»"
    answerIndex: 0
    explanation: "Различение «Shared state (один источник правды) ↔ replicated (копии в разных местах)» лежит в основе дисциплины и определяет, как видеть её объекты."
  - kind: mcq
    prompt: "Какое действие в основе регулярной практики «Непрерывность памяти и состояния»?"
    options:
      - "Любое cross-environment действие — с явным state handle (commit / artifact id)"
      - "Несколько источников правды для одного факта"
      - "Cache без явной инвалидации"
      - "State, мигрирующий между средами «как-нибудь»"
    answerIndex: 0
    explanation: "Регулярная практика — то, что превращает знание в навык. Здесь это: «Любое cross-environment действие — с явным state handle (commit / artifact id)»"
  - kind: mcq
    prompt: "По какому маркеру судить, что «Непрерывность памяти и состояния» работает?"
    options:
      - "Знаешь, где живёт каждый kind state твоих агентов"
      - "Несколько источников правды для одного факта"
      - "Cache без явной инвалидации"
      - "State, мигрирующий между средами «как-нибудь»"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение: «Знаешь, где живёт каждый kind state твоих агентов». Не ощущение, а действие."
  - kind: mcq
    prompt: "Какой инструмент использует дисциплина «Непрерывность памяти и состояния» как опору?"
    options:
      - "Git commit как универсальный handle на state"
      - "Несколько источников правды для одного факта"
      - "Cache без явной инвалидации"
      - "State, мигрирующий между средами «как-нибудь»"
    answerIndex: 0
    explanation: "Шаблон или инструмент удерживает дисциплину между сессиями. Здесь это: «Git commit как универсальный handle на state»"
---

## Зачем

Когда задача проходит через несколько tools и сред (локальный editor → CI → production), state должен быть source-of-truth и явно передаваться, а не «всплывать» в каждом контексте. Конфликтующий контекст = два tool'а имеют разные snapshots. Устаревший контекст = state из прошлой задачи влияет на текущую.

## Ключевые различения

- Shared state (один источник правды) ↔ replicated (копии в разных местах)
- Конфликт (два источника не согласны) ↔ устаревание (один отстал)
- Версионированный state (commit hash) ↔ free-form
- Read-through cache ↔ stale cache

## Инструменты

- Git commit как универсальный handle на state
- Версионированные artifacts (с commit hash, не «latest»)
- Single-source-of-truth для конфигов (один файл, не дубли)
- Cache invalidation by version, не by time

## Практика

- Любое cross-environment действие — с явным state handle (commit / artifact id)
- Один файл = один source-of-truth для конфига
- Регулярная sweep на «брошенный» state (старые branches, кэши)

## Маркеры освоения

- Знаешь, где живёт каждый kind state твоих агентов
- Конфликты состояния ловятся до prod, не в prod

## Антипаттерны

- Несколько источников правды для одного факта
- Cache без явной инвалидации
- State, мигрирующий между средами «как-нибудь»

## Где в стеке

**Память, состояние, выполнение (10-15%).** Какую память даёт агент, как удержать состояние длинной задачи и как избежать дрейфа контекста между шагами и инструментами.
