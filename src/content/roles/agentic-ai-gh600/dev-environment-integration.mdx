---
schemaVersion: 1
slug: dev-environment-integration
title: "Интеграция в среды разработки"
summary: "Как настроить scope агента в твоём dev environment — репо, ветка, env, CI?"
status: published
estimatedMinutes: 30
prerequisites: []
learningOutcomes:
  - "Знаешь scope каждого активного агента (репо/ветка/env)"
  - "Ноль случаев «случайно тронул не ту ветку»"
  - "Восстановление после сбоя = закрытие ветки за 30 секунд"
  - "Выполнять регулярно: Один агент = одна задача = одна ветка"
  - "Выполнять регулярно: Запрет push в protected branches (main, release)"
practice:
  - kind: mcq
    prompt: "По какому из признаков честно судить, что дисциплина «Интеграция в среды разработки» начала работать?"
    options:
      - "Знаешь scope каждого активного агента (репо/ветка/env)"
      - "Full-repo write access без явной причины"
      - "Работа агента в main без gate'ов"
      - "State, который persist'ится между несвязанными задачами"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение, которое появляется регулярно. «Знаешь scope каждого активного агента (репо/ветка/env)» — именно такой признак."
flashcards:
  - front: "Что стоит за различением «Full-repo scope ↔ subdirectory scope ↔ single-file scope»?"
    back: "Full-repo scope ↔ subdirectory scope ↔ single-file scope. Контекст исполнения — это где агент работает: какой репозиторий, какая ветка, какие env vars, какие команды exec'ит, какая инфра доступна"
  - front: "Что стоит за различением «Trunk-based ↔ branch-based work»?"
    back: "Trunk-based ↔ branch-based work. Узкий scope = меньше surface area, легче ревью, дешёвый откат"
  - front: "Что стоит за различением «CI agent (запускается по триггеру) ↔ interactive agent»?"
    back: "CI agent (запускается по триггеру) ↔ interactive agent. Branch-based work = изоляция: если что-то сломалось — просто закрываешь ветку"
  - front: "Что стоит за различением «Ephemeral environment ↔ shared dev env»?"
    back: "Ephemeral environment ↔ shared dev env. Trunk-based с агентами = большие риски на молодой системе governance."
  - front: "По какому маркеру понять, что освоено: «Знаешь scope каждого активного агента (репо/ветка/env)»?"
    back: "Знаешь scope каждого активного агента (репо/ветка/env)"
  - front: "По какому маркеру понять, что освоено: «Ноль случаев «случайно тронул не ту ветку»»?"
    back: "Ноль случаев «случайно тронул не ту ветку»"
masteryQuiz:
  - kind: mcq
    prompt: "Какой вопрос ставит дисциплина «Интеграция в среды разработки» в центр?"
    options:
      - "Как настроить scope агента в твоём dev environment — репо, ветка, env, CI?"
      - "Full-repo write access без явной причины"
      - "Работа агента в main без gate'ов"
      - "State, который persist'ится между несвязанными задачами"
    answerIndex: 0
    explanation: "Дисциплина задаёт фокусирующий вопрос — он определяет, что считать работой, а что — её имитацией. Здесь это: «Как настроить scope агента в твоём dev environment — репо, ветка, env, CI?»"
  - kind: mcq
    prompt: "Что из перечисленного — антипаттерн дисциплины «Интеграция в среды разработки»?"
    options:
      - "Full-repo write access без явной причины"
      - "Один агент = одна задача = одна ветка"
      - "Знаешь scope каждого активного агента (репо/ветка/env)"
      - "Ноль случаев «случайно тронул не ту ветку»"
    answerIndex: 0
    explanation: "Антипаттерн — типовой способ имитировать дисциплину, не осваивая её. В материалах он отмечен явно: «Full-repo write access без явной причины»"
  - kind: mcq
    prompt: "Какое из утверждений — ключевое различение в «Интеграция в среды разработки»?"
    options:
      - "Full-repo scope ↔ subdirectory scope ↔ single-file scope"
      - "Full-repo write access без явной причины"
      - "Работа агента в main без gate'ов"
      - "State, который persist'ится между несвязанными задачами"
    answerIndex: 0
    explanation: "Различение «Full-repo scope ↔ subdirectory scope ↔ single-file scope» лежит в основе дисциплины и определяет, как видеть её объекты."
  - kind: mcq
    prompt: "Какое действие в основе регулярной практики «Интеграция в среды разработки»?"
    options:
      - "Один агент = одна задача = одна ветка"
      - "Full-repo write access без явной причины"
      - "Работа агента в main без gate'ов"
      - "State, который persist'ится между несвязанными задачами"
    answerIndex: 0
    explanation: "Регулярная практика — то, что превращает знание в навык. Здесь это: «Один агент = одна задача = одна ветка»"
  - kind: mcq
    prompt: "По какому маркеру судить, что «Интеграция в среды разработки» работает?"
    options:
      - "Знаешь scope каждого активного агента (репо/ветка/env)"
      - "Full-repo write access без явной причины"
      - "Работа агента в main без gate'ов"
      - "State, который persist'ится между несвязанными задачами"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение: «Знаешь scope каждого активного агента (репо/ветка/env)». Не ощущение, а действие."
  - kind: mcq
    prompt: "Какой инструмент использует дисциплина «Интеграция в среды разработки» как опору?"
    options:
      - "GitHub Codespaces config (`.devcontainer/`) для isolated env"
      - "Full-repo write access без явной причины"
      - "Работа агента в main без gate'ов"
      - "State, который persist'ится между несвязанными задачами"
    answerIndex: 0
    explanation: "Шаблон или инструмент удерживает дисциплину между сессиями. Здесь это: «GitHub Codespaces config (`.devcontainer/`) для isolated env»"
---

## Зачем

Контекст исполнения — это где агент работает: какой репозиторий, какая ветка, какие env vars, какие команды exec'ит, какая инфра доступна. Узкий scope = меньше surface area, легче ревью, дешёвый откат. Branch-based work = изоляция: если что-то сломалось — просто закрываешь ветку. Trunk-based с агентами = большие риски на молодой системе governance.

## Ключевые различения

- Full-repo scope ↔ subdirectory scope ↔ single-file scope
- Trunk-based ↔ branch-based work
- CI agent (запускается по триггеру) ↔ interactive agent
- Ephemeral environment ↔ shared dev env

## Инструменты

- GitHub Codespaces config (`.devcontainer/`) для isolated env
- `.gitattributes` / `.cursorignore` для скрытия от агента
- Branch protection rules + status checks
- Workflow_dispatch для CI-based agents

## Практика

- Один агент = одна задача = одна ветка
- Запрет push в protected branches (main, release)
- Ephemeral environments для длинных задач (preview deployments)

## Маркеры освоения

- Знаешь scope каждого активного агента (репо/ветка/env)
- Ноль случаев «случайно тронул не ту ветку»
- Восстановление после сбоя = закрытие ветки за 30 секунд

## Антипаттерны

- Full-repo write access без явной причины
- Работа агента в main без gate'ов
- State, который persist'ится между несвязанными задачами

## Где в стеке

**Инструменты и среда (20-25%).** Минимально необходимый набор инструментов, MCP-серверы, контекст исполнения и обработка ошибок — поверхности, которые определяют безопасность системы.
