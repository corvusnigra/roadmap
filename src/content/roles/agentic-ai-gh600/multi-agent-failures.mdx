---
schemaVersion: 1
slug: multi-agent-failures
title: "Отказы в многоагентных системах"
summary: "Как обнаружить и восстановиться после частичного сбоя в multi-agent flow?"
status: published
estimatedMinutes: 30
prerequisites: []
learningOutcomes:
  - "Знаешь recovery path для типичных failures своих flows"
  - "Recovery работает без ручного вмешательства в 80% случаев"
  - "Выполнять регулярно: Для каждого handoff — определи поведение при failure"
  - "Выполнять регулярно: Watchdog: если агент молчит >N минут — alert"
  - "Выполнять регулярно: Cascade failures → автоматическая escalation"
practice:
  - kind: mcq
    prompt: "По какому из признаков честно судить, что дисциплина «Отказы в многоагентных системах» начала работать?"
    options:
      - "Знаешь recovery path для типичных failures своих flows"
      - "Один агент завис, остальные ждут вечно"
      - "Cascade без circuit breaker'а"
      - "Recovery «вручную каждый раз»"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение, которое появляется регулярно. «Знаешь recovery path для типичных failures своих flows» — именно такой признак."
flashcards:
  - front: "Что стоит за различением «Hard failure (агент упал) ↔ soft (агент дал плохой выход)»?"
    back: "Hard failure (агент упал) ↔ soft (агент дал плохой выход). Multi-agent failure modes: (1) один агент завис, остальные ждут; (2) один сделал шаг, другие не получили результат; (3) каскад — fail одного триггерит fail других"
  - front: "Что стоит за различением «Localised (один агент) ↔ cascading (распространяется)»?"
    back: "Localised (один агент) ↔ cascading (распространяется). Recovery — три уровня: retry конкретного агента, rollback handoff'а, human escalation"
  - front: "Что стоит за различением «Retry / rollback / escalate — разные стратегии»?"
    back: "Retry / rollback / escalate — разные стратегии. Без явной recovery strategy частичный fail оставляет систему в неконсистентном состоянии."
  - front: "Что стоит за различением «Eventually consistent ↔ strongly consistent recovery»?"
    back: "Eventually consistent ↔ strongly consistent recovery. Multi-agent failure modes: (1) один агент завис, остальные ждут; (2) один сделал шаг, другие не получили результат; (3) каскад — fail одного триггерит fail других. Recovery — три уровня: retry конкретного агента, rollback handoff'а, human escalation. Без явной recovery strategy частичный fail оставляет систему в неконсистентном состоянии."
  - front: "По какому маркеру понять, что освоено: «Знаешь recovery path для типичных failures своих flows»?"
    back: "Знаешь recovery path для типичных failures своих flows"
  - front: "По какому маркеру понять, что освоено: «Recovery работает без ручного вмешательства в 80% случаев»?"
    back: "Recovery работает без ручного вмешательства в 80% случаев"
masteryQuiz:
  - kind: mcq
    prompt: "Какой вопрос ставит дисциплина «Отказы в многоагентных системах» в центр?"
    options:
      - "Как обнаружить и восстановиться после частичного сбоя в multi-agent flow?"
      - "Один агент завис, остальные ждут вечно"
      - "Cascade без circuit breaker'а"
      - "Recovery «вручную каждый раз»"
    answerIndex: 0
    explanation: "Дисциплина задаёт фокусирующий вопрос — он определяет, что считать работой, а что — её имитацией. Здесь это: «Как обнаружить и восстановиться после частичного сбоя в multi-agent flow?»"
  - kind: mcq
    prompt: "Что из перечисленного — антипаттерн дисциплины «Отказы в многоагентных системах»?"
    options:
      - "Один агент завис, остальные ждут вечно"
      - "Для каждого handoff — определи поведение при failure"
      - "Знаешь recovery path для типичных failures своих flows"
      - "Recovery работает без ручного вмешательства в 80% случаев"
    answerIndex: 0
    explanation: "Антипаттерн — типовой способ имитировать дисциплину, не осваивая её. В материалах он отмечен явно: «Один агент завис, остальные ждут вечно»"
  - kind: mcq
    prompt: "Какое из утверждений — ключевое различение в «Отказы в многоагентных системах»?"
    options:
      - "Hard failure (агент упал) ↔ soft (агент дал плохой выход)"
      - "Один агент завис, остальные ждут вечно"
      - "Cascade без circuit breaker'а"
      - "Recovery «вручную каждый раз»"
    answerIndex: 0
    explanation: "Различение «Hard failure (агент упал) ↔ soft (агент дал плохой выход)» лежит в основе дисциплины и определяет, как видеть её объекты."
  - kind: mcq
    prompt: "Какое действие в основе регулярной практики «Отказы в многоагентных системах»?"
    options:
      - "Для каждого handoff — определи поведение при failure"
      - "Один агент завис, остальные ждут вечно"
      - "Cascade без circuit breaker'а"
      - "Recovery «вручную каждый раз»"
    answerIndex: 0
    explanation: "Регулярная практика — то, что превращает знание в навык. Здесь это: «Для каждого handoff — определи поведение при failure»"
  - kind: mcq
    prompt: "По какому маркеру судить, что «Отказы в многоагентных системах» работает?"
    options:
      - "Знаешь recovery path для типичных failures своих flows"
      - "Один агент завис, остальные ждут вечно"
      - "Cascade без circuit breaker'а"
      - "Recovery «вручную каждый раз»"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение: «Знаешь recovery path для типичных failures своих flows». Не ощущение, а действие."
  - kind: mcq
    prompt: "Какой инструмент использует дисциплина «Отказы в многоагентных системах» как опору?"
    options:
      - "Saga pattern для длинных multi-agent flow"
      - "Один агент завис, остальные ждут вечно"
      - "Cascade без circuit breaker'а"
      - "Recovery «вручную каждый раз»"
    answerIndex: 0
    explanation: "Шаблон или инструмент удерживает дисциплину между сессиями. Здесь это: «Saga pattern для длинных multi-agent flow»"
---

## Зачем

Multi-agent failure modes: (1) один агент завис, остальные ждут; (2) один сделал шаг, другие не получили результат; (3) каскад — fail одного триггерит fail других. Recovery — три уровня: retry конкретного агента, rollback handoff'а, human escalation. Без явной recovery strategy частичный fail оставляет систему в неконсистентном состоянии.

## Ключевые различения

- Hard failure (агент упал) ↔ soft (агент дал плохой выход)
- Localised (один агент) ↔ cascading (распространяется)
- Retry / rollback / escalate — разные стратегии
- Eventually consistent ↔ strongly consistent recovery

## Инструменты

- Saga pattern для длинных multi-agent flow
- Compensating transactions (откатывающие действия)
- Watchdog timer на каждого агента
- Human-in-the-loop точки при cascade

## Практика

- Для каждого handoff — определи поведение при failure
- Watchdog: если агент молчит >N минут — alert
- Cascade failures → автоматическая escalation

## Маркеры освоения

- Знаешь recovery path для типичных failures своих flows
- Recovery работает без ручного вмешательства в 80% случаев

## Антипаттерны

- Один агент завис, остальные ждут вечно
- Cascade без circuit breaker'а
- Recovery «вручную каждый раз»

## Где в стеке

**Многоагентная координация (15-20%).** Несколько агентов = новая поверхность отказов. Шаблоны оркестрации, артефакты для аудита, recovery и подстановка/вывод агентов без даунтайма.
