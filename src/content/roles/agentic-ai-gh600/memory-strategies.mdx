---
schemaVersion: 1
slug: memory-strategies
title: "Стратегии памяти агента"
summary: "Какую память даёт агент, и как ограничить её рамками задачи?"
status: published
estimatedMinutes: 30
prerequisites: []
learningOutcomes:
  - "Знаешь, что хранится в long-term memory твоих агентов"
  - "Нет «случайной утечки» prior task context"
  - "Memory снимаешь как PR-артефакт, а не как side-effect"
  - "Выполнять регулярно: Каждая новая задача начинает с чистого state"
  - "Выполнять регулярно: Явный artifact = что сохраняем между задачами (что-то конкретное)"
practice:
  - kind: mcq
    prompt: "По какому из признаков честно судить, что дисциплина «Стратегии памяти агента» начала работать?"
    options:
      - "Знаешь, что хранится в long-term memory твоих агентов"
      - "«Память по умолчанию накапливается»"
      - "Не описана retention policy"
      - "Secrets в context window сохраняются в long-term memory"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение, которое появляется регулярно. «Знаешь, что хранится в long-term memory твоих агентов» — именно такой признак."
flashcards:
  - front: "Что стоит за различением «Short-term (window, до 1M токенов) ↔ long-term (persisted) ↔ external (RAG)»?"
    back: "Short-term (window, до 1M токенов) ↔ long-term (persisted) ↔ external (RAG). Память агента — три типа: короткая (context window), длинная (persisted: файлы, БД), внешняя (RAG, semantic search)"
  - front: "Что стоит за различением «Scope per task ↔ persistent across tasks»?"
    back: "Scope per task ↔ persistent across tasks. Каждый тип имеет своё назначение и риски"
  - front: "Что стоит за различением «Opt-in remember ↔ default-remember (опасно)»?"
    back: "Opt-in remember ↔ default-remember (опасно). Без явных правил жизненного цикла память накапливает шум, секреты и устаревшие факты"
  - front: "Что стоит за различением «Mutable memory (можно перезаписать) ↔ append-only»?"
    back: "Mutable memory (можно перезаписать) ↔ append-only. Дисциплина = явный choice типа памяти для задачи + retention policy + purge schedule."
  - front: "По какому маркеру понять, что освоено: «Знаешь, что хранится в long-term memory твоих агентов»?"
    back: "Знаешь, что хранится в long-term memory твоих агентов"
  - front: "По какому маркеру понять, что освоено: «Нет «случайной утечки» prior task context»?"
    back: "Нет «случайной утечки» prior task context"
masteryQuiz:
  - kind: mcq
    prompt: "Какой вопрос ставит дисциплина «Стратегии памяти агента» в центр?"
    options:
      - "Какую память даёт агент, и как ограничить её рамками задачи?"
      - "«Память по умолчанию накапливается»"
      - "Не описана retention policy"
      - "Secrets в context window сохраняются в long-term memory"
    answerIndex: 0
    explanation: "Дисциплина задаёт фокусирующий вопрос — он определяет, что считать работой, а что — её имитацией. Здесь это: «Какую память даёт агент, и как ограничить её рамками задачи?»"
  - kind: mcq
    prompt: "Что из перечисленного — антипаттерн дисциплины «Стратегии памяти агента»?"
    options:
      - "«Память по умолчанию накапливается»"
      - "Каждая новая задача начинает с чистого state"
      - "Знаешь, что хранится в long-term memory твоих агентов"
      - "Нет «случайной утечки» prior task context"
    answerIndex: 0
    explanation: "Антипаттерн — типовой способ имитировать дисциплину, не осваивая её. В материалах он отмечен явно: ««Память по умолчанию накапливается»»"
  - kind: mcq
    prompt: "Какое из утверждений — ключевое различение в «Стратегии памяти агента»?"
    options:
      - "Short-term (window, до 1M токенов) ↔ long-term (persisted) ↔ external (RAG)"
      - "«Память по умолчанию накапливается»"
      - "Не описана retention policy"
      - "Secrets в context window сохраняются в long-term memory"
    answerIndex: 0
    explanation: "Различение «Short-term (window, до 1M токенов) ↔ long-term (persisted) ↔ external (RAG)» лежит в основе дисциплины и определяет, как видеть её объекты."
  - kind: mcq
    prompt: "Какое действие в основе регулярной практики «Стратегии памяти агента»?"
    options:
      - "Каждая новая задача начинает с чистого state"
      - "«Память по умолчанию накапливается»"
      - "Не описана retention policy"
      - "Secrets в context window сохраняются в long-term memory"
    answerIndex: 0
    explanation: "Регулярная практика — то, что превращает знание в навык. Здесь это: «Каждая новая задача начинает с чистого state»"
  - kind: mcq
    prompt: "По какому маркеру судить, что «Стратегии памяти агента» работает?"
    options:
      - "Знаешь, что хранится в long-term memory твоих агентов"
      - "«Память по умолчанию накапливается»"
      - "Не описана retention policy"
      - "Secrets в context window сохраняются в long-term memory"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение: «Знаешь, что хранится в long-term memory твоих агентов». Не ощущение, а действие."
  - kind: mcq
    prompt: "Какой инструмент использует дисциплина «Стратегии памяти агента» как опору?"
    options:
      - "Claude prompt caching (5-min TTL, дешёвый long-context)"
      - "«Память по умолчанию накапливается»"
      - "Не описана retention policy"
      - "Secrets в context window сохраняются в long-term memory"
    answerIndex: 0
    explanation: "Шаблон или инструмент удерживает дисциплину между сессиями. Здесь это: «Claude prompt caching (5-min TTL, дешёвый long-context)»"
---

## Зачем

Память агента — три типа: короткая (context window), длинная (persisted: файлы, БД), внешняя (RAG, semantic search). Каждый тип имеет своё назначение и риски. Без явных правил жизненного цикла память накапливает шум, секреты и устаревшие факты. Дисциплина = явный choice типа памяти для задачи + retention policy + purge schedule.

## Ключевые различения

- Short-term (window, до 1M токенов) ↔ long-term (persisted) ↔ external (RAG)
- Scope per task ↔ persistent across tasks
- Opt-in remember ↔ default-remember (опасно)
- Mutable memory (можно перезаписать) ↔ append-only

## Инструменты

- Claude prompt caching (5-min TTL, дешёвый long-context)
- Memory tool calls (write/read к файлам)
- Vector DB для semantic recall
- Файловая структура: short-term — `.cache/`, long-term — committed

## Практика

- Каждая новая задача начинает с чистого state
- Явный artifact = что сохраняем между задачами (что-то конкретное)
- Periodic purge (раз в спринт): что устарело? что протухло?

## Маркеры освоения

- Знаешь, что хранится в long-term memory твоих агентов
- Нет «случайной утечки» prior task context
- Memory снимаешь как PR-артефакт, а не как side-effect

## Антипаттерны

- «Память по умолчанию накапливается»
- Не описана retention policy
- Secrets в context window сохраняются в long-term memory

## Где в стеке

**Память, состояние, выполнение (10-15%).** Какую память даёт агент, как удержать состояние длинной задачи и как избежать дрейфа контекста между шагами и инструментами.
