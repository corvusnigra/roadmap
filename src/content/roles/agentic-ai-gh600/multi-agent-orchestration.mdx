---
schemaVersion: 1
slug: multi-agent-orchestration
title: "Оркестрация многоагентных систем"
summary: "Как координировать нескольких агентов и разрешать конфликты их выходов?"
status: published
estimatedMinutes: 35
prerequisites: []
learningOutcomes:
  - "Знаешь, какие шаблоны оркестрации работают на каких задачах"
  - "Конфликты разрешаются предсказуемо"
  - "Выполнять регулярно: Для каждого multi-agent flow — диаграмма handoff'ов"
  - "Выполнять регулярно: Изоляция через branches: каждый агент в своей"
  - "Выполнять регулярно: Conflict resolution стратегия выбирается заранее, не ad-hoc"
practice:
  - kind: mcq
    prompt: "По какому из признаков честно судить, что дисциплина «Оркестрация многоагентных систем» начала работать?"
    options:
      - "Знаешь, какие шаблоны оркестрации работают на каких задачах"
      - "First-write-wins без явной политики"
      - "Скрытые dependencies между агентами"
      - "Один общий workspace без изоляции"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение, которое появляется регулярно. «Знаешь, какие шаблоны оркестрации работают на каких задачах» — именно такой признак."
flashcards:
  - front: "Что стоит за различением «Pipeline (sequential) ↔ parallel ↔ supervisor (hierarchical)»?"
    back: "Pipeline (sequential) ↔ parallel ↔ supervisor (hierarchical). Несколько агентов работают над общей задачей — нужна оркестрация"
  - front: "Что стоит за различением «Изоляция (свой scope) ↔ shared workspace (один репо)»?"
    back: "Изоляция (свой scope) ↔ shared workspace (один репо). Шаблоны: pipeline (последовательный hand-off), parallel-with-merge (изолированные задачи + объединение), supervisor (один агент даёт задачи другим)"
  - front: "Что стоит за различением «Conflict resolution: merge / vote / supervisor decides»?"
    back: "Conflict resolution: merge / vote / supervisor decides. Конфликты неизбежны: одинаковые правки в коде, противоречивые выходы"
  - front: "Что стоит за различением «Synchronous (ждём всех) ↔ async (продолжаем без)»?"
    back: "Synchronous (ждём всех) ↔ async (продолжаем без). Без явной стратегии разрешения = либо хаос, либо first-write-wins (часто худший выбор)."
  - front: "По какому маркеру понять, что освоено: «Знаешь, какие шаблоны оркестрации работают на каких задачах»?"
    back: "Знаешь, какие шаблоны оркестрации работают на каких задачах"
  - front: "По какому маркеру понять, что освоено: «Конфликты разрешаются предсказуемо»?"
    back: "Конфликты разрешаются предсказуемо"
masteryQuiz:
  - kind: mcq
    prompt: "Какой вопрос ставит дисциплина «Оркестрация многоагентных систем» в центр?"
    options:
      - "Как координировать нескольких агентов и разрешать конфликты их выходов?"
      - "First-write-wins без явной политики"
      - "Скрытые dependencies между агентами"
      - "Один общий workspace без изоляции"
    answerIndex: 0
    explanation: "Дисциплина задаёт фокусирующий вопрос — он определяет, что считать работой, а что — её имитацией. Здесь это: «Как координировать нескольких агентов и разрешать конфликты их выходов?»"
  - kind: mcq
    prompt: "Что из перечисленного — антипаттерн дисциплины «Оркестрация многоагентных систем»?"
    options:
      - "First-write-wins без явной политики"
      - "Для каждого multi-agent flow — диаграмма handoff'ов"
      - "Знаешь, какие шаблоны оркестрации работают на каких задачах"
      - "Конфликты разрешаются предсказуемо"
    answerIndex: 0
    explanation: "Антипаттерн — типовой способ имитировать дисциплину, не осваивая её. В материалах он отмечен явно: «First-write-wins без явной политики»"
  - kind: mcq
    prompt: "Какое из утверждений — ключевое различение в «Оркестрация многоагентных систем»?"
    options:
      - "Pipeline (sequential) ↔ parallel ↔ supervisor (hierarchical)"
      - "First-write-wins без явной политики"
      - "Скрытые dependencies между агентами"
      - "Один общий workspace без изоляции"
    answerIndex: 0
    explanation: "Различение «Pipeline (sequential) ↔ parallel ↔ supervisor (hierarchical)» лежит в основе дисциплины и определяет, как видеть её объекты."
  - kind: mcq
    prompt: "Какое действие в основе регулярной практики «Оркестрация многоагентных систем»?"
    options:
      - "Для каждого multi-agent flow — диаграмма handoff'ов"
      - "First-write-wins без явной политики"
      - "Скрытые dependencies между агентами"
      - "Один общий workspace без изоляции"
    answerIndex: 0
    explanation: "Регулярная практика — то, что превращает знание в навык. Здесь это: «Для каждого multi-agent flow — диаграмма handoff'ов»"
  - kind: mcq
    prompt: "По какому маркеру судить, что «Оркестрация многоагентных систем» работает?"
    options:
      - "Знаешь, какие шаблоны оркестрации работают на каких задачах"
      - "First-write-wins без явной политики"
      - "Скрытые dependencies между агентами"
      - "Один общий workspace без изоляции"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение: «Знаешь, какие шаблоны оркестрации работают на каких задачах». Не ощущение, а действие."
  - kind: mcq
    prompt: "Какой инструмент использует дисциплина «Оркестрация многоагентных систем» как опору?"
    options:
      - "GitHub Actions jobs + dependencies — естественная оркестрация"
      - "First-write-wins без явной политики"
      - "Скрытые dependencies между агентами"
      - "Один общий workspace без изоляции"
    answerIndex: 0
    explanation: "Шаблон или инструмент удерживает дисциплину между сессиями. Здесь это: «GitHub Actions jobs + dependencies — естественная оркестрация»"
---

## Зачем

Несколько агентов работают над общей задачей — нужна оркестрация. Шаблоны: pipeline (последовательный hand-off), parallel-with-merge (изолированные задачи + объединение), supervisor (один агент даёт задачи другим). Конфликты неизбежны: одинаковые правки в коде, противоречивые выходы. Без явной стратегии разрешения = либо хаос, либо first-write-wins (часто худший выбор).

## Ключевые различения

- Pipeline (sequential) ↔ parallel ↔ supervisor (hierarchical)
- Изоляция (свой scope) ↔ shared workspace (один репо)
- Conflict resolution: merge / vote / supervisor decides
- Synchronous (ждём всех) ↔ async (продолжаем без)

## Инструменты

- GitHub Actions jobs + dependencies — естественная оркестрация
- Multi-agent frameworks (LangGraph, CrewAI)
- Custom supervisor agent с explicit handoff'ами
- Branch-per-agent + merge через супервизор

## Практика

- Для каждого multi-agent flow — диаграмма handoff'ов
- Изоляция через branches: каждый агент в своей
- Conflict resolution стратегия выбирается заранее, не ad-hoc

## Маркеры освоения

- Знаешь, какие шаблоны оркестрации работают на каких задачах
- Конфликты разрешаются предсказуемо

## Антипаттерны

- First-write-wins без явной политики
- Скрытые dependencies между агентами
- Один общий workspace без изоляции

## Где в стеке

**Многоагентная координация (15-20%).** Несколько агентов = новая поверхность отказов. Шаблоны оркестрации, артефакты для аудита, recovery и подстановка/вывод агентов без даунтайма.
