---
schemaVersion: 1
slug: safe-execution-error-handling
title: "Безопасное выполнение и обработка ошибок"
summary: "Как обработать ошибки tool-вызовов и обеспечить безопасное прерывание long-running agent?"
status: published
estimatedMinutes: 30
prerequisites: []
learningOutcomes:
  - "Ноль агентов «висят» больше 30 минут"
  - "Знаешь точку remediation для типичных fails (сеть, права, конфликты)"
  - "После сбоя — артефакт с трассой, не «что-то сломалось»"
  - "Выполнять регулярно: Лимит попыток ≤5 для любого retry"
  - "Выполнять регулярно: Явный escalation path заранее (не «когда упадёт, тогда разберёмся»)"
practice:
  - kind: mcq
    prompt: "По какому из признаков честно судить, что дисциплина «Безопасное выполнение и обработка ошибок» начала работать?"
    options:
      - "Ноль агентов «висят» больше 30 минут"
      - "Бесконечный retry-loop"
      - "Молчаливый fail без артефакта"
      - "Продолжать после необратимой ошибки"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение, которое появляется регулярно. «Ноль агентов «висят» больше 30 минут» — именно такой признак."
flashcards:
  - front: "Что стоит за различением «Retry с exponential backoff ↔ tight retry loop»?"
    back: "Retry с exponential backoff ↔ tight retry loop. Tool calls фейлятся по сети, по timeout'у, по правам доступа, по бизнес-логике"
  - front: "Что стоит за различением «Transient error (сеть) ↔ permanent (нет прав)»?"
    back: "Transient error (сеть) ↔ permanent (нет прав). Без явной обработки агент входит в retry-loop или галлюцинирует «успех»"
  - front: "Что стоит за различением «Escalation к человеку ↔ continue retry»?"
    back: "Escalation к человеку ↔ continue retry. Дисциплинированный error handling = retry limit + exponential backoff + явный escalation path к человеку"
  - front: "Что стоит за различением «Rollback (отменить шаги) ↔ partial state (оставить как есть)»?"
    back: "Rollback (отменить шаги) ↔ partial state (оставить как есть). Long-running agent должен уметь сохранять checkpoint и восстанавливаться, а не пере-делать с нуля."
  - front: "По какому маркеру понять, что освоено: «Ноль агентов «висят» больше 30 минут»?"
    back: "Ноль агентов «висят» больше 30 минут"
  - front: "По какому маркеру понять, что освоено: «Знаешь точку remediation для типичных fails (сеть, права, конфликты)»?"
    back: "Знаешь точку remediation для типичных fails (сеть, права, конфликты)"
masteryQuiz:
  - kind: mcq
    prompt: "Какой вопрос ставит дисциплина «Безопасное выполнение и обработка ошибок» в центр?"
    options:
      - "Как обработать ошибки tool-вызовов и обеспечить безопасное прерывание long-running agent?"
      - "Бесконечный retry-loop"
      - "Молчаливый fail без артефакта"
      - "Продолжать после необратимой ошибки"
    answerIndex: 0
    explanation: "Дисциплина задаёт фокусирующий вопрос — он определяет, что считать работой, а что — её имитацией. Здесь это: «Как обработать ошибки tool-вызовов и обеспечить безопасное прерывание long-running agent?»"
  - kind: mcq
    prompt: "Что из перечисленного — антипаттерн дисциплины «Безопасное выполнение и обработка ошибок»?"
    options:
      - "Бесконечный retry-loop"
      - "Лимит попыток ≤5 для любого retry"
      - "Ноль агентов «висят» больше 30 минут"
      - "Знаешь точку remediation для типичных fails (сеть, права, конфликты)"
    answerIndex: 0
    explanation: "Антипаттерн — типовой способ имитировать дисциплину, не осваивая её. В материалах он отмечен явно: «Бесконечный retry-loop»"
  - kind: mcq
    prompt: "Какое из утверждений — ключевое различение в «Безопасное выполнение и обработка ошибок»?"
    options:
      - "Retry с exponential backoff ↔ tight retry loop"
      - "Бесконечный retry-loop"
      - "Молчаливый fail без артефакта"
      - "Продолжать после необратимой ошибки"
    answerIndex: 0
    explanation: "Различение «Retry с exponential backoff ↔ tight retry loop» лежит в основе дисциплины и определяет, как видеть её объекты."
  - kind: mcq
    prompt: "Какое действие в основе регулярной практики «Безопасное выполнение и обработка ошибок»?"
    options:
      - "Лимит попыток ≤5 для любого retry"
      - "Бесконечный retry-loop"
      - "Молчаливый fail без артефакта"
      - "Продолжать после необратимой ошибки"
    answerIndex: 0
    explanation: "Регулярная практика — то, что превращает знание в навык. Здесь это: «Лимит попыток ≤5 для любого retry»"
  - kind: mcq
    prompt: "По какому маркеру судить, что «Безопасное выполнение и обработка ошибок» работает?"
    options:
      - "Ноль агентов «висят» больше 30 минут"
      - "Бесконечный retry-loop"
      - "Молчаливый fail без артефакта"
      - "Продолжать после необратимой ошибки"
    answerIndex: 0
    explanation: "Маркер освоения — наблюдаемое поведение: «Ноль агентов «висят» больше 30 минут». Не ощущение, а действие."
  - kind: mcq
    prompt: "Какой инструмент использует дисциплина «Безопасное выполнение и обработка ошибок» как опору?"
    options:
      - "Idempotency keys для tool calls с побочным эффектом"
      - "Бесконечный retry-loop"
      - "Молчаливый fail без артефакта"
      - "Продолжать после необратимой ошибки"
    answerIndex: 0
    explanation: "Шаблон или инструмент удерживает дисциплину между сессиями. Здесь это: «Idempotency keys для tool calls с побочным эффектом»"
---

## Зачем

Tool calls фейлятся по сети, по timeout'у, по правам доступа, по бизнес-логике. Без явной обработки агент входит в retry-loop или галлюцинирует «успех». Дисциплинированный error handling = retry limit + exponential backoff + явный escalation path к человеку. Long-running agent должен уметь сохранять checkpoint и восстанавливаться, а не пере-делать с нуля.

## Ключевые различения

- Retry с exponential backoff ↔ tight retry loop
- Transient error (сеть) ↔ permanent (нет прав)
- Escalation к человеку ↔ continue retry
- Rollback (отменить шаги) ↔ partial state (оставить как есть)

## Инструменты

- Idempotency keys для tool calls с побочным эффектом
- Checkpoint + restore (state в файле перед длинной операцией)
- Escalation paths через PR comment + label
- Circuit breaker pattern для нестабильных внешних API

## Практика

- Лимит попыток ≤5 для любого retry
- Явный escalation path заранее (не «когда упадёт, тогда разберёмся»)
- Failure → observable artifact (PR comment с трассой)

## Маркеры освоения

- Ноль агентов «висят» больше 30 минут
- Знаешь точку remediation для типичных fails (сеть, права, конфликты)
- После сбоя — артефакт с трассой, не «что-то сломалось»

## Антипаттерны

- Бесконечный retry-loop
- Молчаливый fail без артефакта
- Продолжать после необратимой ошибки

## Где в стеке

**Инструменты и среда (20-25%).** Минимально необходимый набор инструментов, MCP-серверы, контекст исполнения и обработка ошибок — поверхности, которые определяют безопасность системы.
